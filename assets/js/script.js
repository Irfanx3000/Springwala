/**
 * script.js - Legacy / Page-specific scripts
 * NOTE: Global mobile menu and sticky header logic moved to app.js to prevent double listeners.
 * Only page-specific drawer logic remains here if not already in app.js.
 */

// All global logic consolidated in app.js for better cross-device consistency.
console.log("script.js loaded (Legacy consolidated into app.js)");
